{
  "name": "wham-explorer",
  "private": true,
  "type": "module",
  "description": "Present only so Node treats js/*.js as ES modules when running the tests. The app itself has no dependencies and no build step.",
  "scripts": {
    "test": "node test/analysis.test.mjs",
    "serve": "python3 -m http.server 8777"
  }
}
